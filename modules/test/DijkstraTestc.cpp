#include <cstdint>
#include <iostream>
#include <vector>

int main()
{
    int64_t N;
    std::cin >> N;
    for (int64_t i = 0; i < N - 1; ++i)
    {
        int64_t a, b, x;
        std::cin >> a >> b >> x;
        std::vector<int64_t> GRAPH;
        // GRAPH[i].push_back(i + 1);
        // GRAPH[i].push_back(x - 1);
    }
}